<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.month6">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activirys.LoginActivity"></activity>

        <service
            android:name=".CacheService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".view.activirys.LineChartActivity" />
        <activity android:name=".view.activirys.MoneyPeopleSetActivity" />
        <activity android:name=".view.activirys.MoreAboutP2PActivity" />
        <activity
            android:name=".view.activirys.GestureActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".view.activirys.MainActivity" />
        <activity android:name=".view.activirys.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="design_width_in_dp"
            android:value="420" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="750" />
    </application>

</manifest>